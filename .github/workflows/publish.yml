name: demo-svc

on:
  push:
    tags:
      - "*"

jobs:
  publish:
    runs-on: ubuntu-20.04
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v3.0.2
      - name: Release
        uses: softprops/action-gh-release@master